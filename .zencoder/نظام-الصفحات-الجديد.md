# نظام الصفحات الجديد - الوثائق الشاملة

## 📋 نظرة عامة

تم إعادة بناء نظام إدارة الطلاب من "فورم إلهي واحد ضخم" إلى **7 صفحات متخصصة ومستقلة**، كل منها لها وظيفة محددة وواضحة.

---

## 🏗️ هيكل الصفحات الجديد

### 1. ✅ صفحة بروفايل الطالب - Dashboard (عرض فقط)
**المسار:** `/students/:studentId/profile`  
**المكون:** `StudentProfileDashboard.tsx`

**الوظيفة:**
- لوحة تحكم مركزية تعرض ملخصات سريعة من جميع الأقسام
- لا توجد تعديلات مباشرة على هذه الصفحة
- تحتوي على روابط سريعة للصفحات المتخصصة

**البيانات المعروضة:**
- بطاقة البيانات الأساسية (الاسم، الصف، ولي الأمر)
- ملخص أكاديمي (المعدل، متوسط الدرجات)
- ملخص مالي (المستحق، المسدد، المتبقي)
- بطاقة الحضور (نسبة الحضور، أيام الغياب)
- بطاقة السلوك (التقييم، المشاكل السلوكية)

**مثال الاستخدام:**
```
مدير المدرسة يبحث عن طالب ويفتح بروفايله
← يرى ملخصاً سريعاً لكل شيء
← إذا أراد التفاصيل، يضغط على الزر المناسب
```

---

### 2. ✏️ صفحة البيانات الأساسية - تعديل فردي
**المسار:** `/students/:studentId/basic-data`  
**المكون:** `BasicDataEditPage.tsx`

**الوظيفة:**
- تعديل فردي لبيانات طالب واحد فقط
- تحتوي على 5 أقسام رئيسية

**الأقسام:**
1. **البيانات الشخصية** - (الاسم، الهوية، الميلاد، النوع، الديانة، احتياجات خاصة)
2. **بيانات القيد الدراسي** - (المرحلة، الفصل، نوع القيد، السنة الأكاديمية)
   - ⚠️ **مهم:** الفصول تتغير ديناميكياً حسب المرحلة (Dependent Dropdowns)
3. **بيانات ولي الأمر** - (الاسم، الهاتف، الوظيفة، إلخ)
4. **بيانات الأم** - (الاسم، الهاتف، الوظيفة، إلخ)
5. **بيانات الطوارئ** - (جهات اتصال طوارئ)

**المميزات:**
- Tabs لتنظيم الأقسام
- التحقق من التغييرات (يظهر عدد التعديلات)
- أزرار حفظ وإلغاء واضحة

**مثال الاستخدام:**
```
موظف شؤون الطلاب يريد تحديث رقم هاتف ولي الأمر
1. يفتح بروفايل الطالب
2. يضغط على "تعديل البيانات الأساسية"
3. ينتقل إلى الصفحة ويختار tab "ولي الأمر"
4. يحدّث الرقم ويضغط "حفظ"
5. النظام يحفظ ويعود للبروفايل
```

---

### 3. 📊 صفحة الإدارة الأكاديمية - تعديل جماعي
**المسار:** `/students/academic-management`  
**المكون:** `AcademicManagementNewPage.tsx`

**الوظيفة:**
- تعديل جماعي لبيانات أكاديمية متعددة الطلاب
- البحث والفلترة الموحد
- إدخال درجات لعدة طلاب دفعة واحدة

**الحقول القابلة للتعديل:**
- المعدل الحالي (GPA)
- إجمالي الدرجات
- متوسط الدرجات
- حالة النجاح

**المميزات:**
- جدول يعرض جميع الطلاب المفلترة
- تعديل مباشر في الجدول
- تحديد الخلايا المعدّلة (خلفية صفراء)
- حفظ دفعي واحد لجميع التعديلات

**مثال الاستخدام:**
```
مدرس يريد إدخال درجات اختبار لصف رابع ابتدائي - فصل A
1. يدخل صفحة الإدارة الأكاديمية
2. يختار المرحلة = "الصف الرابع الابتدائي"
3. يختار الفصل = "4A" (Dependent على المرحلة)
4. يرى قائمة بجميع طلاب 4A
5. يدخل الدرجات لكل طالب في الجدول
6. يضغط "حفظ جميع التعديلات"
7. النظام يحفظ دفعياً
```

---

### 4. 📅 صفحة إدارة الحضور - تعديل جماعي
**المسار:** `/students/attendance-management`  
**المكون:** `AttendanceManagementNewPage.tsx`

**الوظيفة:**
- تسجيل الحضور/الغياب لعدة طلاب دفعة واحدة
- البحث والفلترة الموحد
- عرض تقارير الحضور الشهرية

**الحقول القابلة للتعديل:**
- حالة الحضور (حاضر، غائب، متأخر، معذور)
- التاريخ (يوم محدد)

**المميزات:**
- اختيار التاريخ للتسجيل
- تطبيق حالة واحدة على جميع الطلاب المعروضين (Bulk Action)
- عرض نسبة الحضور الكلية لكل طالب
- عرض عدد أيام الغياب

**مثال الاستخدام:**
```
معلمة تريد تسجيل حضور صفها الجديد
1. تدخل صفحة إدارة الحضور
2. تختار الصف والفصل المطلوب
3. تختار التاريخ الحالي
4. إذا أرادت تسريع العملية: تختار "حاضر" وتضغط "تطبيق على المعروضين"
5. تتحقق من الحالات الخاصة وتعدلها يدوياً
6. تضغط "حفظ جميع التعديلات"
```

---

### 5. 💰 صفحة الإدارة المالية - تعديل جماعي
**المسار:** `/students/finance-management`  
**المكون:** `FinancialManagementNewPage.tsx`

**الوظيفة:**
- تعديل جماعي للبيانات المالية
- البحث والفلترة الموحد
- عرض ملخص مالي شامل

**الحقول القابلة للتعديل:**
- إجمالي المستحق
- المسدد

**الحقول المحسوبة تلقائياً:**
- المتبقي = المستحق - المسدد

**المميزات:**
- جدول يعرض جميع الطلاب والبيانات المالية
- ملخص إحصائي (إجمالي المستحق، المسدد، المتبقي)
- تحديث المتبقي تلقائياً عند كل تعديل

**مثال الاستخدام:**
```
موظف خزينة يريد تحديث حالة المسددات
1. يدخل صفحة الإدارة المالية
2. يختار صف محدد للتركيز عليه
3. يرى جميع طلاب الصف ومستحقاتهم ومسددهم
4. يحدّث عمود "المسدد" للطلاب الذين سددوا
5. يضغط "حفظ جميع التعديلات"
6. النظام يحفظ ويحدّث المتبقي تلقائياً
```

---

### 6. ⭐ صفحة الإدارة السلوكية والإدارية - تعديل جماعي
**المسار:** `/students/behavioral-management`  
**المكون:** `BehavioralAdministrativeManagementNewPage.tsx`

**الوظيفة:**
- تعديل جماعي للبيانات السلوكية والإدارية
- البحث والفلترة الموحد
- عرض إحصائيات السلوك

**الحقول القابلة للتعديل:**
- تقييم السلوك (ممتاز، جيد جداً، جيد، مقبول، ضعيف)
- وجود مشاكل سلوكية (نعم/لا)
- مستوى المشاركة (عالي، متوسط، منخفض)

**المميزات:**
- ملخص إحصائي (عدد الطلاب بكل تقييم)
- تحديث سريع للحقول باستخدام select dropdowns

---

### 7. 📝 صفحة السجل - عرض فقط
**المسار:** `/students/:studentId/log`  
**المكون:** `LogViewPage.tsx`

**الوظيفة:**
- عرض سجل كامل لجميع التغييرات التي طرأت على بيانات الطالب
- صفحة أرشيفية للتدقيق والمراجعة

**المعلومات المعروضة:**
- التاريخ والوقت الدقيق للتغيير
- نوع التغيير (القسم الذي تم تعديله)
- من قام بالتغيير
- سبب التغيير (إن وجد)
- تفاصيل كاملة: القيمة القديمة والقيمة الجديدة لكل حقل

**المميزات:**
- ملخص إحصائي (إجمالي التغييرات، آخر تحديث)
- جدول قابل للفرز والبحث
- Dialog يعرض تفاصيل كل تغيير

---

## 🔗 Shared Components و Hooks

### 1. Hook: `useStagesAndClasses.ts`
**الغرض:** إدارة المراحل والفصول الديناميكية

```typescript
// الاستخدام:
const stages = useStages(); // جميع المراحل
const classes = useClassesByStage(stage); // الفصول الخاصة بمرحلة معينة

// البيانات:
STAGES_AND_CLASSES_DATA = {
  'KG1': ['KG1A', 'KG1B', 'KG1C', 'KG1D'],
  'الصف الأول الابتدائي': ['1A', '1B', '1C', '1D'],
  // ... إلخ
}
```

### 2. Hook: `useUnifiedSearch.ts`
**الغرض:** توحيد منطق البحث والفلترة

```typescript
// الاستخدام:
const {
  filters,           // الفلاتر الحالية
  setStudentName,    // دالة تحديث اسم الطالب
  setStage,          // دالة تحديث المرحلة
  setClass,          // دالة تحديث الفصل
  clearFilters,      // دالة مسح جميع الفلاتر
  filteredStudents,  // الطلاب المفلترة
  resultsCount,      // عدد النتائج
  hasActiveFilters,  // هل هناك فلاتر فعالة
} = useUnifiedSearch(students);
```

### 3. Component: `UnifiedSearchAndFilter.tsx`
**الغرض:** مكون البحث والفلترة الموحد

```typescript
// يتم استخدامه في جميع الصفحات الجماعية
<UnifiedSearchAndFilter
  studentName={filters.studentName}
  onStudentNameChange={setStudentName}
  stage={filters.stage}
  onStageChange={setStage}
  class={filters.class}
  onClassChange={setClass}
  onClearFilters={clearFilters}
  hasActiveFilters={hasActiveFilters}
  resultsCount={resultsCount}
/>
```

---

## 🎯 قواعد المنطق غير قابلة للتجاهل

### ✅ 1. Dependent Dropdowns: المرحلة والفصل

**القاعدة:** لا يوجد قائمة فصول ثابتة. الفصول تعتمد حصرياً على المرحلة المختارة.

```typescript
// صحيح ✅
المرحلة: "الصف الرابع الابتدائي" → الفصول: [4A, 4B, 4C, 4D]

// خطأ ❌
عرض جميع الفصول من جميع المراحل في نفس الوقت
```

### ✅ 2. منطق البحث الموحد

**القاعدة:** نفس منطق البحث والفلترة يُستخدم في جميع الصفحات.

```typescript
// تطبيق موحد:
1. البحث النصي: يبحث عن جزء من اسم الطالب
2. الفلترة بالمرحلة: تصفية جميع الطلاب في مرحلة واحدة
3. الفلترة بالفصل: تصفية الطلاب في فصل محدد (يعتمد على المرحلة)
```

### ✅ 3. تعديلات فردية vs جماعية

**القاعدة واضحة:**
- **صفحة البيانات الأساسية**: تعديل فردي فقط (طالب واحد)
- **الصفحات الأخرى**: تعديل جماعي (عدة طلاب)

---

## 🚀 كيفية التكامل مع التطبيق الحالي

### المسارات المطلوبة في `App.tsx`:

```typescript
import { StudentProfileDashboard } from '@/pages/students/StudentProfileDashboard';
import { BasicDataEditPage } from '@/pages/students/BasicDataEditPage';
import { AcademicManagementNewPage } from '@/pages/students/AcademicManagementNewPage';
import { AttendanceManagementNewPage } from '@/pages/students/AttendanceManagementNewPage';
import { FinancialManagementNewPage } from '@/pages/students/FinancialManagementNewPage';
import { BehavioralAdministrativeManagementNewPage } from '@/pages/students/BehavioralAdministrativeManagementNewPage';
import { LogViewPage } from '@/pages/students/LogViewPage';

// أضف المسارات:
<Route path="/students/:studentId/profile" element={<StudentProfileDashboard />} />
<Route path="/students/:studentId/basic-data" element={<BasicDataEditPage />} />
<Route path="/students/academic-management" element={<AcademicManagementNewPage />} />
<Route path="/students/attendance-management" element={<AttendanceManagementNewPage />} />
<Route path="/students/finance-management" element={<FinancialManagementNewPage />} />
<Route path="/students/behavioral-management" element={<BehavioralAdministrativeManagementNewPage />} />
<Route path="/students/:studentId/log" element={<LogViewPage />} />
```

---

## 🔄 تدفق البيانات والتحديثات التلقائية

### عند إجراء تعديلات:

1. **المستخدم يعدّل بيانات في صفحة متخصصة**
   ↓
2. **النظام يحفظ التغييرات في Supabase**
   ↓
3. **يتم تسجيل التغيير في جدول `student_audit_trail`**
   ↓
4. **عند العودة للبروفايل الرئيسي، يتم تحميل البيانات المحدثة تلقائياً**

---

## 📌 ملاحظات مهمة

1. **عدم فقدان البيانات:** جميع البيانات الموجودة محفوظة وآمنة
2. **الحفاظ على العلاقات:** جميع العلاقات بين الجداول محفوظة
3. **التوافق:** النظام الجديد متوافق تماماً مع قاعدة البيانات الحالية
4. **الأداء:** تحميل البيانات المطلوبة فقط (وليس كل البيانات دفعة واحدة)
5. **الأمان:** فصل واضح للصلاحيات بناءً على نوع الصفحة

---

## 🎓 أمثلة عملية

### سيناريو 1: مدرس يدخل درجات اختبار

```
1. الدخول إلى: صفحة الإدارة الأكاديمية
2. البحث والفلترة:
   - المرحلة: "الصف الرابع الابتدائي"
   - الفصل: "4A" (تلقائياً حسب المرحلة)
3. جدول يعرض: 25 طالب (مثلاً)
4. إدخال الدرجات: في الخلايا الخاصة بكل طالب
5. الحفظ: ضغطة واحدة على "حفظ جميع التعديلات"
6. النتيجة: جميع الدرجات محفوظة، والبروفايل مُحدّث تلقائياً
```

### سيناريو 2: موظف شؤون الطلاب يحدّث البيانات

```
1. البحث عن الطالب واختياره
2. فتح البروفايل (Dashboard): يرى ملخصاً
3. ضغط على "تعديل البيانات الأساسية"
4. التنقل بين الـ Tabs:
   - البيانات الشخصية
   - القيد الدراسي (مع Dependent dropdowns)
   - ولي الأمر
   - الأم
   - الطوارئ
5. تعديل البيانات المطلوبة
6. ضغط "حفظ"
7. العودة التلقائية للبروفايل مع البيانات المحدثة
```

---

## ✨ الخلاصة

النظام الجديد يوفر:
- ✅ **تصميم منطقي** واضح الفصل بين الوظائف
- ✅ **تجربة مستخدم محسنة** (لا أكثر من الحقول المزدحمة)
- ✅ **أداء أفضل** (تحميل البيانات المطلوبة فقط)
- ✅ **أمان أعلى** (فصل الصلاحيات والحقول)
- ✅ **دعم العمليات الجماعية** (تعديل عدة طلاب دفعة واحدة)
- ✅ **توثيق كامل** للتغييرات (Audit Trail)